# {{PROJECT_NAME}}

> Replace the placeholders ({{like_this}}) with your real values, then
> delete this callout.

Short one-paragraph summary of what this project is. Not a status
report — that lives in `CURRENT-STATE.md`. This is identity: what is
the thing, what does it exist to accomplish, where does it live.

## Identity

| Field | Value |
|---|---|
| **Project name (this folder)** | `{{project-folder-name}}` |
| **Type** | {{e.g., production service, internal tool, personal experiment}} |
| **Canonical source repo** | `{{repo URL or path}}` |
| **Primary stack** | {{e.g., Python/FastAPI, Rust, Node, AzerothCore 3.3.5a}} |
| **Deployment target** | {{e.g., AWS, local dev, customer site}} |
| **Active focus** | {{one line — what's the major effort right now}} |
| **Test user / account** | {{e.g., Greznyk char, test@acme.com}} |
| **Project root on disk** | `{{path}}` |

## Read order for a fresh session

1. **`CURRENT-STATE.md`** ← what's happening RIGHT NOW
2. **`AUDIT-LOG.md`** (top ~10–20 entries) ← recent meaningful changes
3. **`STATE-SNAPSHOT.md`** ← what's installed and configured for THIS project
4. **`OPEN-DEFECTS.md`** ← known issues
5. **`DECISION-LOG.md`** (relevant entries) ← why things are the way they are
6. Browse **`how-to/`** ← procedures we've documented
7. For deeper architecture / mechanics context, see `shared/reference/`
   (linked from `INDEX.md`)

## What lives where (top-level on-disk layout)

```
{{project-root-on-disk}}/
├── {{dir-1}}/           {{purpose}}
├── {{dir-2}}/           {{purpose}}
└── ...
```

## Quick links

- {{external doc 1}}: `{{path or URL}}`
- {{external doc 2}}: `{{path or URL}}`
- Related shared docs: see `INDEX.md` (search for project keywords)
